#include "main.h"
#include <math.h>
/**
 * _pow_recursion - function returns rased power
 * @x: vaue
 * @y: value
 * Return: value of exponent
 */
int _pow_recursion(int x, int y)
{
int z;

if (y < 0)
return (-1);
else
z = _pow_recursion(x,y)
return (z);
}
