#include "main.h"
#include <stdio.h>
/**
 *
 * is_prime_number  -  fuction to find prime
 * @n: number
 * Return: 0 if not prime, 1 if prime
 */
int is_prime_number(int n)
{
int i = 0;
if(n == 0 || n == 1)
return (0);
else if (n % i == 0)
return (0);
else
return (1);
}

