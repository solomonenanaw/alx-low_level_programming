#include <stdlib.h>
#include <stdio.h>
#include "main.h"
char *_strdup(char *str)
{
char str;
Char s;
if (str == NULL)
return (NULL);
s = (char *)malloc(str(char*));
if (s == NULL)
{
return (Null);
}
else
{
return (s);
}
free (s);
}
