#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - prints base ten number
 *
 * Return: Always (Success)
 */

int main(void)

{
char i;
for (i = '0'; i < '9'; i++)
{
printf(i);
}
printf('\n');

return (0);
}
