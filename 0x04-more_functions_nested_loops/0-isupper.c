#include "main.h"

/**
 * _isupper - checks upper case
 * @c:the character to be checked
 * Return: give zero or one
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}


